{
  "name": "doorman-showcase",
  "version": "1.0.0",
  "private": true,
  "description": "Static GitHub Pages showcase and its browser acceptance suite",
  "scripts": {
    "serve": "http-server . -a 127.0.0.1 -p 4173 -c-1",
    "test": "playwright test",
    "test:routes": "playwright test tests/routes.spec.ts"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "^1.60.0",
    "@types/node": "^26.3.0",
    "http-server": "^14.1.1"
  }
}
